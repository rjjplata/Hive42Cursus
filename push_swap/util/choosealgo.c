#include "../push_swap.h"

void pushtoalgo(int *array)
{
    int elemnum;

    elemnum = ft_arraylength(array);

    if(elemnum == 3)
        random3(array);
    if(elemnum == 5)
    else
        printf("wrong value");
}