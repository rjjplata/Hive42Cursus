#include "../includes/push_swap.h"

int pushtoalgo(int length)
{
    if(length == 3)
        return(1);
    else if(length == 5)
        return(2);
    else
        printf("wrong value");
    return(0);
}